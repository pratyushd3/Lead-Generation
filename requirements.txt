fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
python-dotenv>=1.0
httpx>=0.27

# LLM providers (optional - only the configured one is used at runtime)
openai>=1.30
anthropic>=0.34

# Outreach mailer (optional)
resend>=2.0

# Marketplace payments (optional)
stripe>=10.0
